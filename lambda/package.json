{
  "name": "wsse-student-api",
  "version": "1.0.0",
  "description": "Student Enrollment API Lambda Function with DynamoDB",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.709.0",
    "@aws-sdk/lib-dynamodb": "^3.709.0"
  }
}
